---
name: Reproduction problem
about: Something doesn't install, import, or reproduce as documented
title: "[repro] "
labels: ["reproduction"]
---

**Environment**
- OS:
- Python version:
- Package version (`python -c "from importlib.metadata import version; print(version('orbital-thermal'))"`):
- Commit or release (`v1.0.0` or `main`):
- Install method (editable `-e .`, wheel, `[fluids]` extra?):

**Exact command run**

```bash

```

**Expected output**

**Actual output (full traceback)**

```text

```
